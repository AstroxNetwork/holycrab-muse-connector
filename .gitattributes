# Normalise to LF in the repo: the Dockerfile, shell snippets in docs and
# the CI workflow all assume LF, and a CRLF Dockerfile breaks on Linux.
* text=auto eol=lf

*.png binary
*.webp binary
*.jpg binary
package-lock.json -diff linguist-generated
